
// 1. 関数宣言を行い、実行された時にアラート表示させるよう処理を作ってください。
function showAlertMessage(){
  alert("Alertを表示しています。")
}

// showAlertMessage()

// 2. 関数式内で計算した結果を返し、それを外部から実行してコンソールで表示させてください。

var mathFunc = function(){
  return 1000+100+10+1
};

console.log(mathFunc());
// 3. スコープついて調べて自分なりの言葉でまとめてください。
// スコープとは、変数や定数を使用できる範囲のこと。関数の内側で定義した定数・変数はその関数内では使用できるが、外側で使用することは出来ない。一見不便だが、関数外で使用できないその性質上、バグを防ぐ等便利な点もある。